node_modules/
.DS_Store

# Reference tree snapshots (generated by scripts/consolidate_reference.py).
# Run the script locally to produce these; not committed to avoid diff churn.
reference/_snapshot.md
reference/_snapshot.json

# Raw export archives dropped into mirror/ingest/ for processing.
# Contents can contain personal data; only the .processed filename marker is committed.
mirror/ingest/*/
